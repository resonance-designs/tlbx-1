// theme.slint
export struct RDSTheme {
    background_main: color,
    background_raised: color,
    background_hover: color,
    border_subtle: color,
    border_strong: color,
    text_primary: color,
    text_secondary: color,
    accent_primary: color,
    accent_secondary: color,
    accent_highlight: color,
    meter_normal: color,
    meter_warning: color,
    meter_clip: color,
    radius_small: length,
    radius_medium: length,
    radius_large: length,
    font_body: string,
    font_readout: string,
    font_icons: string,
    font_small: length,
    font_medium: length,
    font_large: length,
    ui_header_label_bg: color,
    ui_header_label_txt: color,
    ui_label_bg: color,
    ui_label_txt: color,
    knob_body_svg: image,
    knob_indicator_svg: image
}
